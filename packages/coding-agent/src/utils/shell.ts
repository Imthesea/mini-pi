/**
 * Shell utilities.
 * 从 pi 项目 utils/shell.ts 抄来（V1 最小化）。
 * 🔴 V1 桩——所有功能后续实现（killTrackedDetachedChildren, getShellConfig, getShellEnv 等）。
 */

/** 🔴 V1 桩：SIGTERM 时 kill detached children */
export function killTrackedDetachedChildren(): void {}
